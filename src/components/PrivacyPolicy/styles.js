import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  PolicyContainer: {
    marginTop: '100px',
    marginBottom: '6em',
  },
  ContentPaper: {
    padding: '2em',
    marginBottom: '2em'
  },
  List: {
    paddingLeft: '2em'
  }
}));
